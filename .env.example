DATABASE_URL="postgresql://user:password@localhost:5432/meavo_gateway"

AUTH_SECRET="generate-with: openssl rand -base64 32"
AUTH_URL="http://localhost:3000"

ADMIN_EMAILS="you@company.com"
ADMIN_PASSWORD="change-me-in-production"

# Vacation Tracker sync (optional locally; required in production)
HOLS_SYNC_URL="https://hols.meavo.app"
GATEWAY_SYNC_SECRET="generate-with: openssl rand -base64 32"
